name: build-and-push

# Construye backend + frontend de kbia EN GitHub → GHCR; deploy en el VPS por
# webhook (pull + up). Sin builds en el VPS. Ver spcapps-infra/docs/DEPLOY-MODEL.md
# (El frontend hornea NEXT_PUBLIC_API_URL=https://kbia.spcapps.com en su Dockerfile,
#  por eso NO se pasa build-arg aquí.)
on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build & push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ghcr.io/spc74-hub/kbia-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/spc74-hub/kbia-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy on VPS (webhook → pull + up)
        run: |
          # --retry: si el aviso al webhook se pierde por un parpadeo de red, el job
          # sale en ROJO con la imagen ya construida y subida — y el deploy no llega a
          # empezar. Pasó con luuktrainer el 2026-08-29. --retry-all-errors es
          # necesario: sin él, curl solo reintenta errores de transporte, no un 5xx.
          curl -fsS --retry 3 --retry-delay 5 --retry-all-errors -X POST https://webhook.spcapps.com/ci-deploy \
            -H "X-Deploy-Token: ${{ secrets.DEPLOY_WEBHOOK_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"app":"kbia"}'
