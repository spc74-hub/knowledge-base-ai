[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci --force"]
cacheDirectories = []

[phases.build]
cmds = ["rm -rf .next && npm run build"]

[start]
cmd = "npm run start"
